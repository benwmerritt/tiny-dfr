# R1 adversarial touch-safety review — 2026-07-02

Scope: commits `8100b75^..838aa97` (overlay stack, region layout, close
semantics + drain, Cancel handling). Full report generated by the review
agent; verdict and actionable findings recorded here.

## Verdict

**SAFE TO INSTALL.** 0 BLOCKER, 0 FIX-NOW. No constructible event sequence
strands a virtual key against the reviewed code. All 12 adversarial checklist
items (overlay transitions, timeout ordering, tap-outside, strip-tap close,
Fn flip/swap, config reload, Cancel, constrained re-hit, anchor resurrection,
double-fire, slot overwrite, rogue set_active) verified OK.

## Findings and disposition

- **F1 (nice-to-have, pre-M5 requirement)**: `drain_touches` skips entries on
  unreachable sets without key-up. Safe today (nothing bumps
  `strip_generation` in production), but the M5 live-strip rebuild MUST drain
  strip touches BEFORE bumping the generation — make it structural via one
  `rebuild_strip` method. → comment added at the skip branch; ordering
  requirement carried into M5 scope.
- **F2 (nice-to-have, hardening)**: `mark_visible_set_changed_and_unpressed`
  force-cleared `pressed` without key-up, masking any future release into a
  no-op. → FIXED: renamed `mark_visible_set_changed`, no longer writes
  `pressed`, `debug_assert`s the drain invariant instead.
- **F3 (nice-to-have, hardening)**: unconditional `touches.insert` on Down
  could overwrite a live entry (relies on libinput slot contract). → FIXED:
  Down now releases-and-removes any existing entry for the slot first.
- **F4 (nice-to-have, UX)**: Classic-mode overlays have no tap-outside escape
  (F-key layer + `OverlayTimeoutMs = 0` + no CloseOverlay button = Fn-flip
  only). No key-safety impact. → not fixed; unreachable on Ben's machine
  (Fn layer dead, see .scratch/fn-layer-keyd), timeout enabled by default.
- **F5 (style)**: insert-then-get round-trip on Down. → FIXED alongside F3.
